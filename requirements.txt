# Pinned for reproducibility (tested with Python 3.10–3.12)
numpy==2.1.3
pandas==2.2.3
scipy==1.14.1          # optional: exact hypergeometric in concordance (a pure-python fallback is built in)
requests==2.32.3       # fetch_data.py only
